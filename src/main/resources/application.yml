spring:
  application:
    name: prova_web
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/prova_web?useTimezone=true&serverTimezone=UTC
    username: root
    password: Seeloko153247,
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect